# GROMACS Protein-Only Test

This folder contains a lightweight protein-only example for the MSTBx GROMACS workflow.

```bash
./RunTest.sh
```

The script downloads 1UBQ, prepares three replicas, writes the MSTBx-style GROMACS layout, and stops before running simulations.
